# Copy to deploy.env (gitignored) and edit to override publish.ps1 defaults.
# CLI args to publish.ps1 still take precedence over these.

PiHost=wormhole.local
PiUser=jared
RemoteDir=/home/jared/tapo-viewer
